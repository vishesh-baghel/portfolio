# OpenAI API Key (required for Mastra agent)
OPENAI_API_KEY=your_openai_api_key_here

# Mastra Database (optional - LibSQL file-based storage)
# By default, uses:
# - Local dev: file:.mastra/db.sqlite
# - Vercel build: file::memory:?cache=shared (in-memory)
# - Vercel runtime: file:/tmp/mastra-prod.sqlite
# 
# You can override with a custom path or use Turso for persistent cloud storage:
# MASTRA_DB_FILE=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=your-auth-token
#
# Note: The .mastra directory is gitignored and will be created automatically in local dev

# Observability (optional - for Vercel deployment)
# Vercel automatically configures OTEL when @vercel/otel is installed
# No additional env vars needed for basic tracing

# PostHog Analytics (optional - for user behavior tracking)
# Get your key from https://posthog.com
NEXT_PUBLIC_POSTHOG_KEY=your_posthog_project_api_key
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
