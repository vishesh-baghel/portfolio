import React from 'react';

const ContactSection = () => {
  return (
    <section className="py-10 font-mono">
      <h2 className="text-lg font-bold cursor-default text-text-primary mt-0 mb-2">contact</h2>
      <p className="text-sm max-w-xl">
        open to freelance/consulting: small scopes, clear outcomes, fast iterations.
      </p>
    </section>
  );
};

export default ContactSection;
